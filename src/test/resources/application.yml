server:
  port : 80

build.name: abc-tech-service
build.version: "1.0"

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa